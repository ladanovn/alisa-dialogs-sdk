export declare function getHeaders({ oAuthToken }: {
    oAuthToken: any;
}): {
    Authorization: string;
    'Content-type': string;
};
export default class ImagesApi {
    skillId: string;
    oAuthToken: string;
    constructor({ oAuthToken, skillId }: {
        oAuthToken: any;
        skillId: any;
    });
    uploadImage(imageUrl: string): Promise<any>;
    getImages(): Promise<any>;
    private checkProps;
}
