import Session from './session';
declare class Sessions {
    sessions: {};
    constructor();
    add(session: any): any;
    find(session: any): {};
    findById(sessionId: string): any;
    findOrCreate(sessionId: string): any;
    readonly length: number;
    removeById(sessionId: any): boolean;
    /**
     * Remove all sessions
     */
    flush(): void;
}
export default Sessions;
export { Sessions, Session };
